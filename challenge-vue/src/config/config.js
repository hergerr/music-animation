/**
 * Window size in samples that is used when performing a
 * Fast Fourier Transform (FFT) to get frequency domain data.
 */
const FFT_SIZE = 512;

/**
 * Audio animation refresh frequency (Hz)
 */
const FREQUENCY = 40;

export { FFT_SIZE, FREQUENCY };
